import { ArrowUpRight, Star } from 'lucide-react';
import { Link } from 'react-router-dom';
export const money = (value) => `₹${Number(value || 0).toLocaleString('en-IN')}`;
export function SectionTitle({ eyebrow, title, description, action }) { return <div className="mb-7 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="mb-2 text-xs font-bold uppercase tracking-[.18em] text-indigo-600">{eyebrow}</p><h2 className="text-2xl font-bold tracking-tight text-slate-900 sm:text-3xl">{title}</h2>{description && <p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">{description}</p>}</div>{action}</div>; }
export function HotelCard({ hotel, search }) { const room = hotel.rooms?.find((item) => item.isAvailable !== false) || hotel.rooms?.[0]; const dynamic = Math.round((room?.basePrice || 0) * 1.12); return <article className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-xl"><img src={hotel.images?.[0]} alt={hotel.name} className="h-52 w-full object-cover"/><div className="p-5"><div className="mb-3 flex items-start justify-between gap-3"><div><h3 className="font-bold text-slate-900">{hotel.name}</h3><p className="mt-1 text-sm text-slate-500">{hotel.location?.city} · {hotel.location?.address}</p></div><span className="flex items-center gap-1 rounded-lg bg-amber-50 px-2 py-1 text-sm font-semibold text-amber-700"><Star size={14} fill="currentColor"/>{hotel.rating}</span></div><div className="mb-4 flex flex-wrap gap-2">{hotel.amenities?.slice(0, 3).map((item) => <span key={item} className="rounded-full bg-slate-100 px-2.5 py-1 text-xs text-slate-600">{item}</span>)}</div><div className="flex items-end justify-between border-t border-slate-100 pt-4"><div><p className="text-xs text-slate-500">Current price / night</p><p className="text-xl font-bold text-slate-900">{money(dynamic)} <span className="text-sm font-normal text-slate-400">from {money(room?.basePrice)}</span></p></div><Link to={`/hotels/${hotel._id}${search ? `?${search}` : ''}`} className="rounded-lg bg-slate-900 p-2.5 text-white hover:bg-indigo-600"><ArrowUpRight size={18}/></Link></div></div></article>; }
export function StatusPill({ children }) { const style = { Confirmed: 'bg-emerald-50 text-emerald-700', Paid: 'bg-emerald-50 text-emerald-700', Pending: 'bg-amber-50 text-amber-700', Cancelled: 'bg-rose-50 text-rose-700', Refunded: 'bg-slate-100 text-slate-600', Active: 'bg-indigo-50 text-indigo-700', Available: 'bg-emerald-50 text-emerald-700', Occupied: 'bg-amber-50 text-amber-700', Maintenance: 'bg-rose-50 text-rose-700', High: 'bg-rose-50 text-rose-700', Medium: 'bg-amber-50 text-amber-700', Low: 'bg-emerald-50 text-emerald-700' }; return <span className={`inline-flex rounded-full px-2.5 py-1 text-xs font-semibold ${style[children] || 'bg-slate-100 text-slate-600'}`}>{children}</span>; }
export function Table({ columns, rows }) { return <div className="overflow-x-auto rounded-xl border border-slate-200 bg-white"><table className="w-full min-w-[700px] text-left text-sm"><thead className="border-b border-slate-200 bg-slate-50 text-xs uppercase tracking-wide text-slate-500"><tr>{columns.map((column) => <th key={column} className="px-5 py-3 font-semibold">{column}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{rows}</tbody></table></div>; }
