import { CheckCircle2, Download, Printer } from 'lucide-react';
import { Link, useSearchParams } from 'react-router-dom';
import { PublicNav } from '../../components/AppShell';
import { money } from '../../components/Ui';
export default function Confirmation() { const [params] = useSearchParams(); return <><PublicNav/><main className="grid min-h-[75vh] place-items-center bg-slate-50 px-5 py-12"><section className="w-full max-w-lg rounded-2xl border border-slate-200 bg-white p-7 text-center shadow-sm"><span className="mx-auto grid h-14 w-14 place-items-center rounded-full bg-emerald-100 text-emerald-600"><CheckCircle2 size={30}/></span><p className="mt-5 text-xs font-bold uppercase tracking-widest text-emerald-600">Booking confirmed</p><h1 className="mt-2 text-2xl font-bold">Your stay is reserved.</h1><p className="mt-2 text-sm text-slate-500">A confirmation has been prepared for your booking.</p><div className="mt-7 divide-y divide-slate-100 rounded-xl bg-slate-50 px-5 text-left text-sm">{[['Booking ID',params.get('id') || 'BK-10482'],['Hotel',params.get('hotel') || 'Grand Orchid Hotel'],['Room',params.get('room') || 'Deluxe'],['Stay',`${params.get('checkIn') || '18 Sep'} – ${params.get('checkOut') || '21 Sep'}`],['Guests',params.get('guests') || '2'],['Total',money(params.get('total') || 17280)],['Status','Confirmed']].map(([label,value]) => <p key={label} className="flex justify-between gap-4 py-3"><span className="text-slate-500">{label}</span><span className="font-semibold text-slate-800">{value}</span></p>)}</div><div className="mt-6 flex gap-3"><Link to="/dashboard" className="flex-1 rounded-lg bg-indigo-600 px-4 py-3 text-sm font-semibold text-white">View booking</Link><button onClick={() => window.print()} className="rounded-lg border border-slate-200 p-3 text-slate-600"><Printer size={18}/></button><button className="rounded-lg border border-slate-200 p-3 text-slate-600"><Download size={18}/></button></div></section></main></>; }
