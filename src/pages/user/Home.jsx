import { CalendarDays, MapPin, Search, Sparkles, Users } from 'lucide-react';
import { useState } from 'react';
import { useNavigate } from 'react-router-dom';
import { mockHotels } from '../../data/mockData';
import { HotelCard, SectionTitle } from '../../components/Ui';
import { PublicNav } from '../../components/AppShell';
export default function Home() { const navigate = useNavigate(); const [form, setForm] = useState({ city: '', checkIn: '', checkOut: '', guests: '2' }); const submit = (e) => { e.preventDefault(); navigate(`/hotels?${new URLSearchParams(form)}`); }; return <><PublicNav/><main><section className="relative isolate overflow-hidden bg-slate-950 py-20 sm:py-28"><div className="absolute inset-0 -z-10 bg-[linear-gradient(115deg,rgba(15,23,42,.94),rgba(15,23,42,.55)),url('https://images.unsplash.com/photo-1542314831-068cd1dbfeeb?auto=format&fit=crop&w=2000&q=85')] bg-cover bg-center"/><div className="mx-auto max-w-7xl px-5"><p className="mb-5 flex items-center gap-2 text-sm font-semibold text-indigo-200"><Sparkles size={16}/> BETTER STAYS, SMARTER PRICES</p><h1 className="max-w-3xl text-4xl font-bold tracking-tight text-white sm:text-6xl">Find your next stay at the right price.</h1><p className="mt-5 max-w-xl text-lg leading-8 text-slate-200">Discover exceptional hotels and see transparent, demand-aware room prices before you book.</p><form onSubmit={submit} className="mt-10 grid gap-3 rounded-2xl bg-white p-4 shadow-2xl md:grid-cols-[1.25fr_1fr_1fr_.8fr_auto]"><Field icon={<MapPin size={18}/>} label="Where" value={form.city} placeholder="City or destination" change={(city) => setForm({...form, city})}/><Field icon={<CalendarDays size={18}/>} label="Check in" type="date" value={form.checkIn} change={(checkIn) => setForm({...form, checkIn})}/><Field icon={<CalendarDays size={18}/>} label="Check out" type="date" value={form.checkOut} change={(checkOut) => setForm({...form, checkOut})}/><Field icon={<Users size={18}/>} label="Guests" value={form.guests} type="select" change={(guests) => setForm({...form, guests})}/><button className="flex items-center justify-center gap-2 rounded-xl bg-indigo-600 px-5 py-3.5 text-sm font-bold text-white hover:bg-indigo-700"><Search size={18}/>Search</button></form></div></section><section className="mx-auto max-w-7xl px-5 py-18"><SectionTitle eyebrow="Handpicked for you" title="Featured stays" description="Comfort, character, and live price transparency."/><div className="grid gap-6 md:grid-cols-3">{mockHotels.map((hotel) => <HotelCard key={hotel._id} hotel={hotel}/>)}</div></section><section className="bg-slate-900 px-5 py-16 text-white"><div className="mx-auto grid max-w-7xl gap-10 md:grid-cols-2"><div><p className="text-xs font-bold uppercase tracking-[.18em] text-indigo-300">Dynamic pricing, explained</p><h2 className="mt-3 text-3xl font-bold">Clear rates that respond to real demand.</h2><p className="mt-4 max-w-lg leading-7 text-slate-300">Each dynamic price is returned by our pricing service using availability, check-in date and local demand. You see both the base rate and current recommendation—no hidden maths in the interface.</p></div><div className="grid grid-cols-3 gap-3">{[['Demand','High'],['Occupancy','85%'],['Tonight','₹5,760']].map(([a,b]) => <div key={a} className="rounded-xl bg-white/10 p-5"><p className="text-sm text-slate-300">{a}</p><p className="mt-2 text-xl font-bold">{b}</p></div>)}</div></div></section></main></>; }
function Field({ icon, label, value, change, type = 'text', placeholder }) { return <label className="flex min-w-0 items-center gap-3 rounded-xl px-3 py-2 hover:bg-slate-50"><span className="text-indigo-600">{icon}</span><span className="min-w-0 flex-1"><span className="block text-xs font-semibold text-slate-500">{label}</span>{type === 'select' ? <select value={value} onChange={(e) => change(e.target.value)} className="mt-1 w-full bg-transparent text-sm font-medium outline-none"><option value="1">1 guest</option><option value="2">2 guests</option><option value="3">3 guests</option><option value="4">4 guests</option></select> : <input required={label === 'Where'} type={type} value={value} onChange={(e) => change(e.target.value)} placeholder={placeholder} className="mt-1 w-full bg-transparent text-sm font-medium outline-none"/>}</span></label>; }
